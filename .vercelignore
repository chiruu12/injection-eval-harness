# The page is built at deploy time by scripts/build_site.py from results/, and
# the function serves those same files. Everything else is the harness, which has
# no business in a bundle whose only job is to render a table it already has.
.github/
.venv/
data/
docs/
src/
tests/
uv.lock
Dockerfile
Makefile
